#!/bin/sh
set -e
# Only remove data on real uninstall, not on upgrade/reinstall
if [ "$1" = "remove" ]; then
    rm -f /etc/enigma2/homematicccu_settings.json || true
    rm -f /etc/enigma2/homematicccu_devices.json  || true
fi
exit 0
